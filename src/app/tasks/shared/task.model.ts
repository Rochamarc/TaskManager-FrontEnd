export class Task{
    public id: number;
    public title: string;
  
    // Crio um construtor
    // Ja estudei POO nao, se nao precisa de explicaçao pra isso
    constructor(id: number, title: string){
      this.id = id;
      this.title = title;
    }
  }
  
  